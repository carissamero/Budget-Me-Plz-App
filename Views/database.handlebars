<br /><br /><br /><br /><br /><br /><br />
{{#each data}}
  <div>
    <div
      class="flex-1 h-full max-w-xlg mx-auto opacity-95 bg-white rounded-lg shadow-xl"
    >
      <div class="flex flex-col md:flex-row">
        <div
          class="md:flex-1 md:h-full md:max-w-lg md:mx-auto items-center justify-center p-6 sm:p-12 md:w-1/2"
        >
          <div class="w-full">
            <h1 class="mb-4 text-2xl font-bold text-center text-gray-700">
              {{first_name}}
              {{last_name}}
            </h1>
            <div>
              <label class="block font-bold mt-2 text-sm">
                Email
              </label>
              <input
                class="w-full px-4 py-2 text-sm border rounded-md focus:border-blue-400 focus:outline-none focus:ring-1 focus:ring-blue-600"
                placeholder="{{email}}"
              />
            </div>
            <div class="mt-4">
              <label class="block font-bold text-sm">
                Password
              </label>
              <input
                type="password"
                class="w-full px-4 py-2 text-sm border rounded-md focus:border-blue-400 focus:outline-none focus:ring-1 focus:ring-blue-600"
                placeholder="{{password}}"
                type="password"
              />
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <br />
{{/each}}